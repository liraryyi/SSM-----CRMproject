<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.liraryyi.crm.workbench.dao.CustomerRemarkDao">

    <insert id="saveCustomerRemark">
        insert into tbl_contacts_remark
        (
        id,
        noteContent,
	    createTime,
	    createBy,
	    editTime,
	    editBy,
	    editFlag,
	    customerId
        ) values (
        #{id},
        #{noteContent},
        #{createTime},
        #{createBy},
        #{editTime},
        #{editBy},
        #{editFlag},
        #{customerId}
        )
    </insert>
  	
</mapper>

































